import {View, Text} from 'react-native';
import React from 'react';

interface Props {}

const DropDownPickerComponent = (props: Props) => {
  const {} = props;
  return (
    <View>
      <Text>DropDownPickerComponent</Text>
    </View>
  );
};

export default DropDownPickerComponent;
